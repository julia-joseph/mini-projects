import { LandingHoverDirective } from './landing-hover.directive';

describe('LandingHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new LandingHoverDirective();
    expect(directive).toBeTruthy();
  });
});
